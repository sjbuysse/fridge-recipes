import React, { Fragment } from 'react';
import { SnackbarContainer } from '../features/snackbar';

const App: React.FC = () => {
    return (
        <Fragment>
            <SnackbarContainer/>
        </Fragment>
    );
}

export default App;
